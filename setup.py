from setuptools import find_packages, setup

setup(
    name='loggerz',
    packages=find_packages(include=['loggerz', 'loggerz.*']),
    version='0.1.0',
    description='A logger utility',
    author='Zanna_37',
    license='MIT',
    install_requires=[],
    setup_requires=[],
    tests_require=[],
)
